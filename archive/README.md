# Archive — NOT context

Files here were removed from the `docs/` context corpus and are preserved for
history only. **Never cite anything in this directory as fact about Tricentis
data, the models, or the agent.** If the dashboard / win-probability /
call-sentiment work returns to this repo's scope, the relevant file gets
reviewed, updated, and moved back under `docs/` with a routing row in
`docs/README.md` — it does not get read from here.

Archived 2026-08-12: the six files under `archive/docs/` describe the old GTM
intelligence-dashboard project, whose pipeline (`run.py`, notebook model,
`dashboard/index.html`) does not exist in this repo.
